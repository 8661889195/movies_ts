import styled from 'styled-components';

export const ListStyle = styled.ul`
  display: flex;
  flex-wrap: wrap;
  column-gap: 8px;
  row-gap: 8px;
  
  margin: 0;
  padding: 0;
  margin-bottom: 10px;
  margin-top: 10px;
`